



{% for notice in app.flashes('notice') %}

      {% if notice  %}
        <div x-data="{show:true}" x-init="setTimeout(()=>show=false, 3000)"x-show="show" class="fixed top-0 left-1/2 transform -translate-x-1/2 bg-sky-500 text-white px-48 py-3"> 
       <p>
        {{ notice }}
       </p>
  </div>
      {% endif %}
{% endfor %}