  <!-- Hero -->
        <section
            class="relative h-72 bg-black flex flex-col justify-center align-center text-center space-y-4 mb-4"
        >
            <div
                class="absolute top-0 left-0 w-full h-full opacity-10 bg-no-repeat bg-center"
                style="background-image: url('images/lg.jpg')"

            ></div>

            <div class="z-10">
                <h1 class="text-6xl font-bold uppercase text-white">
                    Symfo<span class="text-blue-500">Gigs</span>
                </h1>
                <p class="text-2xl text-gray-200 font-bold my-4">
                    Find or post Symfony jobs & projects
                </p>
                <div>
                   {% if app.user %}
                   
                    {% else %}

                     <a
                        href="/register"
                        class="inline-block border-2 border-white text-white py-2 px-4 rounded-xl uppercase mt-2 hover:text-blue-500 hover:border-blue-500"
                        >Sign Up to List a Gig</a
                    >

                     {% endif %}
                </div>
            </div>
        </section